$(document).on('click', '#campaign-delete', function() {
    return confirm("Are you sure you want to delete this campaign? It will be gone forever!")
});

$(document).on('click', '#chapter-delete', function() {
    return confirm("Are you sure you want to delete this chapter? It will be gone forever!")
});

$(document).on('click', '#section-delete', function() {
    return confirm("Are you sure you want to delete this section? It will be gone forever!")
});